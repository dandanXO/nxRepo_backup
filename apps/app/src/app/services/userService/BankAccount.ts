export interface BankAccount {
  bankAccount?: string;      // 銀行帳戶
  bankId?: number;           // 銀行帳戶流水號
  bankName?: string;         // 銀行名稱
  holderName?: string;       // 持有人姓名
  main?: boolean;            // 是否為主卡
}
