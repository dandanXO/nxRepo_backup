import {ISVGComponent} from "../../ISVGComponent";

export const KeySvg = (props: ISVGComponent) => {
  return (
      <img className={props.className} src={"assets/001/icon_password.png"} alt="Phone Icon" />
  )
}
